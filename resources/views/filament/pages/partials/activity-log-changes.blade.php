<div class="space-y-3 text-sm">@forelse ($changes ?? [] as $field => $change)<div class="rounded-lg border p-3"><div class="font-semibold">{{ $field }}</div><div class="text-gray-500">Before: {{ is_scalar($change['before'] ?? null) ? ($change['before'] ?? '-') : json_encode($change['before'] ?? null) }}</div><div>After: {{ is_scalar($change['after'] ?? null) ? ($change['after'] ?? '-') : json_encode($change['after'] ?? null) }}</div></div>@empty <p class="text-gray-500">No changed values recorded.</p>@endforelse</div>
